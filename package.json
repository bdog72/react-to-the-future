{
  "private": true,
  "scripts": {
    "deploy": "surge ./public --domain react-to-the-future.bdog72.surge.sh",
    "start": "browser-sync start --server 'public' --files 'public'"
  },
  "devDependencies": {
    "browser-sync": "^2.18.5",
    "stylelint": "^7.6.0",
    "stylelint-config-standard": "^15.0.0",
    "surge": "^0.18.0"
  }
}
